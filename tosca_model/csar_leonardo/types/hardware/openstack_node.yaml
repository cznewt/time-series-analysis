tosca_definitions_version: tosca_simple_yaml_1_0

description: OpenStack compute node

node_types:
  cmdb.nodes.compute.OpenStackNode:
    derived_from: tosca.nodes.Compute
    attributes:
      attr_list:
        type: map
        entry_schema:
          type: string

